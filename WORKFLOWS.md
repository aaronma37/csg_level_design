* Creating an asset: Create asset given description.  After iterating on the asset, then make sure its added to the tavern scene and compile the tavern scene.

*NOTE* The view_vox.sh script does not work currently. Don't use that
